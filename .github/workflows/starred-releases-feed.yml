name: Starred Releases Feed

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: starred-releases-feed
  cancel-in-progress: false

jobs:
  feed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: gh-pages
          path: pages

      - name: Update feed
        uses: ras0q/starred-releases-feed@main
        with:
          token: ${{ secrets.GH_PAT }}
          state-path: pages/state.json
          feed-path: pages/starred-releases.atom
          feed-url: https://ras0q.github.io/starred-releases.atom
          max-runtime-minutes: 10
          min-remaining-points: 100

      - name: Publish gh-pages
        working-directory: pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add starred-releases.atom state.json
          git diff --staged --quiet || git commit -m "Update starred releases feed"
          git push origin gh-pages
